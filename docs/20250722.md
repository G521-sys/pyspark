#-----------------------7.22日报----------------------
#1.tms物流已完成
#ods 
mysql导入到hive
#dwd，
.交易域订单明细事务事实表(已完成)
2.交易域支付成功事务事实表
3.交易域取消运单事务事实表
4.物流域揽收事务事实表
5.物流域发单事务事实表
6.物流域转运完成事务事实表
7.物流域派送成功事务事实表
8.物流域签收事务事实表
9.交易域运单累积快照事实表
10.物流域运输事务事实表
11.中转域入库事务事实表
12.中转域分拣事务事实表
13.中转域出库事务事实表
#dim，
园区维度表：dim_complex_full
快递员维度表：dim_express_courier_full
机构维度表：dim_organ_full（修正父机构名称逻辑）
区域维度表：dim_region_full（简单直接，保留原逻辑）
班次维度表：dim_shift_full（左连接关联字典表）
司机维度表：dim_truck_driver_full（保留原左连接逻辑）
车辆维度表：dim_truck_full（左连接关联所有维度）
用户地址维度表：dim_user_address_zip（时间转换修正）
用户维度表：dim_user_zip（时间戳转换修正）
#dws，
最近 1 日完成运输次数
-- 最近 1 日完成运输里程
-- 最近 1 日完成运输时长
-- 最近 1 日各城市完成运输次数
-- 最近 1 日各城市完成运输里程
-- 最近 1 日各城市完成运输时长
-- 最近 1 日各机构完成运输次数
-- 最近 1 日各机构完成运输里程
-- 最近 1 日各机构完成运输时长
-- 最近 1 日各类卡车完成运输次数
-- 最近 1 日各类卡车完成运输里程
-- 最近 1 日各类卡车完成运输时长
-- 被依赖的
-- 最近 1 日各机构各类卡车完成运输次数
-- 最近 1 日各机构各类卡车完成运输里程
-- 最近 1 日各机构各类卡车完成运输时长
ads，
-- 1.1 运单相关统计
-- 最近 1/7/30 日接单总数
-- 最近 1/7/30 日接单金额
-- 最近 1/7/30 日发单总数
-- 最近 1/7/30 日发单金额
-- 1.2 运输综合统计
-- 最近 1/7/30 日完成运输次数
-- 最近 1/7/30 日完成运输里程
-- 最近 1/7/30 日完成运输时长
--2. 运单主题
   -- 2.1 运单综合统计
   -- 最近 1/7/30 日下单数
   -- 最近 1/7/30 日下单金额
-- 2.2 各类型货物运单统计
-- 最近 1/7/30 日各类型货物下单数
-- 最近 1/7/30 日各类型货物下单金额